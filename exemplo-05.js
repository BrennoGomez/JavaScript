console.log("Olá, motorista!")
var velocidade = 99.5
console.log(`A velocidade do veículo é: ${velocidade}km/h.`) //fazendo uso de interpolação (``)

//condição simples, utilizando apenas um if.
if(velocidade >= 80) {
    console.log("Você ultrapassou a velocidade da via e foi multado.")
} 

console.log("Respeite sempre as leis de trânsito e use o cinto de segurança!")